{
  "name": "prototyperReactFramer",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "concurrently \"cd server && meteor npm install\" \"cd client && npm install\"",
    "start": "concurrently \"cd server && meteor -p 9000\" \"cd client && npm start\""
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "author": "Andrew Liebchen",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^3.1.0"
  }
}
